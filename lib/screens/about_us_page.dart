import 'package:flutter/material.dart';

class AboutUsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('كيف بدأنا')),
      body: Center(child: Text('معلومات حول كيف بدأنا')),
    );
  }
}
